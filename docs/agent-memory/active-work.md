# Active Work

Workflow: the Strix + DeepSeek V4 Flash follow-up remediation from scan
`bilibili-mcp_e8df` is complete in the inherited `0a1b` worktree. The user
explicitly required one Paseo-managed Claude Code implementation agent; Codex
then ran independent standards/spec review and sent four same-scope repair
rounds back to that same agent. This executor instruction superseded the
direct-Codex-only rule for this follow-up task only; it does not change the
historical evidence for the 38 findings remediated on 2026-07-30. The frozen
contracts and final evidence are
`docs/agent-memory/handoffs/2026-08-05-strix-deepseek-security-remediation-task-ticket.md`
and
`docs/agent-memory/handoffs/2026-08-05-strix-deepseek-security-remediation-codex-to-claude.md`,
with the final Claude report and QA record beside them.
No commit, push, PR, tag, version bump, release, publication, real Cookie use,
live Bilibili traffic, or ASR model download/switch is authorized.

Completed scope: the six grouped verdicts in the validated
report
`C:\Users\ZX\.codex\visualizations\2026\07\29\019fadc5-5ec9-7d82-bd93-06e12a69d8da\strix-deepseek-v4-flash-20260805\strix_runs\bilibili-mcp_e8df\validated-security-report.md`:
one confirmed Medium untrusted-content boundary, one confirmed Low validation
error disclosure, two conditional Medium ASR symlink weaknesses, one
conditional Low subtitle-port admission gap, and five installed-but-unreachable
dependency advisories grouped under one compatible SDK/lock refresh. Final
Codex verification passes: build; 39 files / 803 tests; production audit with
zero vulnerabilities; 180-file / 788,704-byte package dry run with no forbidden
paths; 95 focused stdio/tool/handler tests; diff check; zero staged files; zero
ASR state temp residue; and zero high-confidence literal credential assignments.
HEAD remains `ab4dd02854f0483fc7668c713523b4be77de6cc7` and the inherited dirty
baseline remains user-owned and uncommitted.

The ten-tool product boundary remains fixed. ASR is still explicit,
default-off, native-subtitle-first, and ready-state-only. The local doctor still
reports no ready model, so no model was downloaded or switched and no live ASR
end-to-end smoke was run. All Phase 1-3 and remediation changes remain
uncommitted and unpublished. GitHub Issue #22 is released and closed.

Status: `v1.10.1` is the current npm latest and GitHub Release. Annotated tag `v1.10.1` resolves to documentation release commit `3aee13d`; Actions run `30233179604` passed install, 405 tests, build, and trusted publication. Runtime behavior remains the ten-tool `v1.10.0` surface, while the bilingual README and Hero SVGs now foreground complete Favorites cursor traversal and user-controlled downstream processing.

Provider history: Issue #22 first used the user's real GLM terminal route and
switched to DeepSeek only after a confirmed five-hour quota response. The ASR
Phase 1 and Phase 2 handoffs used the user's selected DeepSeek route. Phase 3
was completed directly by Codex after the existing Paseo daemon was unavailable.
The current 38-finding remediation is also direct Codex execution by explicit
user instruction, not a Paseo fallback.
No Claude Code implementation-provider model was hard-coded in repository
configuration or handoff files; this is separate from the product's intentionally
pinned ASR model allowlist.

The pre-existing, review-gated `docs/agent-memory/pending-learning-proposals.md` modification remains preserved and outside Issue #22.

Files under `docs/superpowers/` are historical records only. They are not current instructions and must not trigger any `superpowers:*` skill.

Controlled-learning proposals remain manual. GitHub ticket progress does not enable local phase-count reminders.

## Future Direction — Not Active

The ASR and CLI working tree has reached a verified but unpublished boundary.
Revisit MCP protocol modernization only as a separate GitHub Issue:

1. Preserve the public wire-level legacy baseline now covered by `initialize` → `tools/list` → `tools/call`; extend it only as part of the separate migration ticket.
2. Convert the dated MCP research into a dual-era acceptance matrix covering modern `server/discover`, required result/cache fields, and legacy fallback.
3. Only then migrate to the TypeScript SDK v2 dual-era stdio path.
4. Evaluate optional annotations, icons, additional structured outputs, Tasks, MRTR, or HTTP transport features only when a real product need exists.

Sources: `docs/research/2026-07-29-mcp-protocol-update.md`, `docs/research/2026-07-29-mcp-tools-evolution.md`, and the 2026-07-29 entries in `docs/agent-memory/decisions.md`.

This direction is not active work and does not authorize a code change, GitHub Issue, implementation-agent launch, commit, release, or protocol-support claim.
