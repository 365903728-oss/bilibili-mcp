# Active Work

Workflow: Matt Pocock skills with GitHub Issues and bounded file-backed execution contracts. Codex owns planning, provider routing, review, and final verification; one Paseo-managed Claude Code agent may implement the frozen handoff.

Active work: no feature or release task is currently active. GitHub Issue #22 is released and closed. GitHub Issue #20 remains a separate Node engine-floor compatibility follow-up.

Status: `v1.10.0` is the current npm latest and GitHub Release. Annotated tag `v1.10.0` resolves to release commit `7ff2257`; Actions run `30230653151` passed install, 405 tests, build, and trusted publication. The published package exposes ten MCP tools, including `list_bilibili_favorite_videos` for current-account, all-created-Folder discovery with one fixed 20-row upstream page per call and opaque continuation.

Execution: GLM was selected first through the user's real terminal routing and stopped only after a confirmed five-hour quota response. DeepSeek then completed the same bounded handoff through an explicit provider matching the user's `DeepSeek` terminal semantics. No model choice was hard-coded in repository files.

The pre-existing, review-gated `docs/agent-memory/pending-learning-proposals.md` modification remains preserved and outside Issue #22.

Files under `docs/superpowers/` are historical records only. They are not current instructions and must not trigger any `superpowers:*` skill.

Controlled-learning proposals remain manual. GitHub ticket progress does not enable local phase-count reminders.
