# Active Work

Workflow: Matt Pocock skills with GitHub Issues, bounded Codex-to-Claude handoffs, and Paseo CLI execution.

Active work: v1.8.0 is published. GitHub Issue #20 is the next untriaged compatibility follow-up.

Status: Annotated tag `v1.8.0` points to `4be845f`; Actions run `30193180970` passed install, 299 tests, build, and trusted npm publication. npm latest is `1.8.0` with SLSA provenance, the exact-version CLI smoke passes, and the GitHub Release is non-draft/non-prerelease. Three production advisories are cleared; one underlying moderate Hono `serveStatic` advisory remains statically unreachable and awaits an upstream Node 18-compatible SDK fix.

Codex launches and reviews one bounded Claude Code implementation agent through Paseo. The user does not manually transfer prompts between Codex and Claude Code.

Files under `docs/superpowers/` are historical records only. They are not current instructions and must not trigger any `superpowers:*` skill.

Controlled-learning proposals remain manual. GitHub ticket progress does not enable local phase-count reminders.
