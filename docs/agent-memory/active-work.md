# Active Work

Workflow: Matt Pocock skills with GitHub Issues and bounded file-backed execution contracts. For Issue #21, the user explicitly replaced Paseo/Claude execution with direct Codex implementation.

Active work: No active feature ticket. GitHub Issue #20 remains the next separate, untriaged Node engine-floor compatibility follow-up.

Status: `v1.9.0` is published. Commit `b77c3fc` is on `master`; the annotated tag triggered successful Actions run `30195477401`; npm `latest` is `1.9.0` with SLSA provenance; the GitHub Release is non-draft/non-prerelease; and Issue #21 is closed. The only remaining worktree change is the pre-existing, review-gated learning proposal, which was excluded from both release commits.

The prepared Codex-to-Claude handoff remains a historical contract only. No Claude-authored report is expected; the direct execution result is recorded in `docs/agent-memory/handoffs/2026-07-26-bilibili-video-search-codex-report.md`.

Files under `docs/superpowers/` are historical records only. They are not current instructions and must not trigger any `superpowers:*` skill.

Controlled-learning proposals remain manual. GitHub ticket progress does not enable local phase-count reminders.
