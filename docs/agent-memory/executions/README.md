# Execution Reports

Store substantial three-adapter execution and acceptance reports here using:

```text
YYYY-MM-DD-<task>-<mode>-report.md
```

Use the unified template in `../agent-communication.md`. The GitHub Issue is
the planning source; these reports record execution and verification evidence,
not a duplicate ticket. Collaboration-only handoffs remain under `../handoffs/`.
