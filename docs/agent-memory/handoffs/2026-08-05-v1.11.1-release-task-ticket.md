# Task Ticket: v1.11.1 AI Subtitle ID Fix Release

## Ticket

- ID: `REL-2026-08-05-V1.11.1`
- Title: Publish the merged AI subtitle ID fix as v1.11.1
- Status: `done` (published and independently verified 2026-08-05)
- Owner: `Codex`
- Source: User authorization on 2026-08-05; GitHub Issue #24; merged PR #25

## Objective

Prepare the smallest patch release that publishes the already-merged AI
subtitle ID fix, credits `@CYL-collab` in both changelogs and the GitHub
Release, and preserves every existing MCP/runtime boundary.

## Scope

In scope:

- Bump package and lockfile versions from `1.11.0` to `1.11.1`.
- Add matching Chinese and English `v1.11.1` changelog sections.
- Credit `@CYL-collab` for reporting Issue #24 and contributing PR #25.
- Create the release research, QA, handoff, verification, and memory records.
- Verify, commit, push `master`, create/push annotated tag `v1.11.1`, monitor
  trusted npm publication, create a bilingual GitHub Release, and verify the
  public artifact.

Out of scope:

- Any runtime, test, MCP schema, dependency, README, badge, MCP Toplist,
  workflow, ASR, credential, or unrelated documentation change.
- Rewriting the PR merge commit or Git history.

## Files To Inspect Or Edit

Expected edit:

- `package.json`
- `package-lock.json`
- `CHANGELOG.md`
- `CHANGELOG_EN.md`
- `docs/research/2026-08-05-v1.11.1-publishing.md`
- `docs/qa/2026-08-05-v1.11.1-release.md`
- scoped files under `docs/agent-memory/`

Do not touch:

- `src/`, `tests/`, `.github/workflows/publish.yml`, `README.md`,
  `README_EN.md`, or `docs/agent-memory/pending-learning-proposals.md`.

## Required Capabilities

- Claude Code `package-maintainer` for the version/package boundary.
- `secret-scanning` for the release/package boundary.
- Codex `release-verifier` and `risk-reviewer` before publication acceptance.
- Local `npm`, `git`, and `gh`; current official GitHub Actions/npm docs.

## Acceptance Criteria

- [x] Package and lockfile report exactly `1.11.1`.
- [x] Both changelogs describe only the merged fix and credit `@CYL-collab`.
- [x] Build, all tests, production audit, package dry run, diff/encoding/secret
      checks, and package entry-point checks pass.
- [x] Package contents exclude source, tests, agent memory, QA/research,
      credentials, local config, and obsolete Smithery files.
- [x] The ten-tool MCP surface, dependencies, and publish workflow are unchanged.
- [x] Annotated tag `v1.11.1` points to the release commit on remote `master`.
- [x] Trusted-publishing Actions succeeds; npm `latest` is `1.11.1` with
      provenance; exact-version `npx` smoke passes.
- [x] The non-draft, non-prerelease GitHub Release is bilingual and thanks
      `@CYL-collab`.
- [x] No credential, Cookie, token, private value, or unrelated dirty file is
      committed or published.

## Verification

```powershell
npm ci
npm run build
npm test
npm audit --omit=dev --json
npm pack --dry-run --json --ignore-scripts
git diff --check
```

After publication: inspect the Actions run, npm version/dist-tags/integrity/
provenance, the remote annotated tag target, the GitHub Release, and isolated
`npx -y @xzxzzx/bilibili-mcp@1.11.1` version/help output.

## Stop And Report Conditions

- Any source/test/dependency/workflow change becomes necessary.
- Version/tag/npm `1.11.1` is no longer available.
- Build, tests, package boundary, audit, or secret checks fail unexpectedly.
- Trusted publishing or public artifact verification fails.
