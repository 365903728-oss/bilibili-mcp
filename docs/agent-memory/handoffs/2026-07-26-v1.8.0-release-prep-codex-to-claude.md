# Codex To Claude Handoff: v1.8.0 Release Preparation

## Update Goal

Implement GitHub Issue [#18](https://github.com/XZXZZX-Ai/bilibili-mcp/issues/18): prepare the source tree for v1.8.0, the **Structured Transcript Evidence** release.

This release packages the already-merged Issue #16 structured transcript output and Issue #17 citation-ready transcript source/timestamp links. It is a release-preparation task, not a new feature task.

## Current Judgment

- Branch: `master`.
- Base HEAD and `origin/master`: `390a5edfa735aa803f607f04e9f90a1ebddc7ba8`.
- Current package and lockfile version: `1.7.2`.
- Current npm latest and GitHub Release: `1.7.2`.
- Public MCP tool count: 8.
- Last independently verified full suite after Issue #17: 299 tests.
- The structured output and evidence-link implementation, tests, and concise README field documentation are already merged.
- The tag-triggered `.github/workflows/publish.yml` already uses a GitHub-hosted runner, OIDC, Node 22.14.0, npm 11.18.0, tests, build, and direct provenance publication. Its v1.7.2 run succeeded.
- No tag, npm publication, GitHub Release, commit, or push is authorized by this handoff.

Planning and evidence sources:

- GitHub Issue #18
- `docs/research/2026-07-26-v1.8.0-release-github-source-learning.md`
- `docs/agent-memory/active-work.md`
- `docs/agent-memory/project-facts.md`
- `docs/agent-memory/decisions.md`
- `docs/agent-memory/harness-security.md`

The source-learning decision register is binding:

- **adopt** explicit package/lock version parity and separate build/test/package/registry gates;
- **adapt** upstream OIDC guidance to the existing direct `npm publish --provenance --access public` workflow;
- **reject** semantic-release migration, third-party publishing Actions, and workflow upgrades in this ticket;
- **unresolved** npm account-side trusted-publisher configuration remains a separately authorized release-time check.

## Recommended Approach

Make the smallest source-preparation change:

1. Use npm's no-tag version command to synchronize only package metadata.
2. Add matching Chinese and English v1.8.0 changelog entries for Issue #16 and Issue #17.
3. Update the two README release links and the verified current test count.
4. Run package and release verification without changing runtime code, tests, dependencies, or the publish workflow.
5. Write the required Claude report and stop for Codex review.

## Files To Inspect

- `AGENTS.md`
- `CLAUDE.md`
- `package.json`
- `package-lock.json`
- `CHANGELOG.md`
- `CHANGELOG_EN.md`
- `README.md`
- `README_EN.md`
- `.github/workflows/publish.yml`
- `src/server.ts`
- `src/server/tool-schemas.ts`
- `src/server/tool-handlers.ts`
- `tests/server-tools.test.ts`
- `tests/server-handler-sanitization.test.ts`
- `tests/bilibili-transcript.test.ts`
- `docs/research/2026-07-26-v1.8.0-release-github-source-learning.md`
- `docs/agent-memory/agent-communication.md`
- `docs/agent-memory/codemap.md`
- `docs/agent-memory/harness-security.md`

## Files To Edit

Expected release-preparation files:

- `package.json`
- `package-lock.json`
- `CHANGELOG.md`
- `CHANGELOG_EN.md`
- `README.md`
- `README_EN.md`

Write the final report to:

- `docs/agent-memory/handoffs/2026-07-26-v1.8.0-release-prep-claude-report.md`

No other file should change.

## Required Capabilities

- Use the project `package-maintainer` subagent to review package/lock version parity, dependency stability, and dry-run package contents.
- Use the project `release-verifier` subagent after preparation to run the bounded release-readiness checklist.
- Apply the repository's credential and secret-scan rules to the intended release diff and dry-run package.
- If a required subagent stalls, use a bounded wait, perform the same checklist at top level, and report the stall truthfully. Do not create an autonomous agent tree.

`product-requirements`, `system-design`, `domain-modeling`, `codebase-design`, `vitest`, and `test-baseline-builder` are intentionally not required: the public behavior and terminology are already approved and merged, this task changes no interface or module seam, and it adds or modifies no test. Running the existing Vitest suite remains mandatory.

## Constraints

- Version target is exactly `1.8.0`.
- Changelog date is exactly `2026-07-26`.
- Changelog content must describe:
  - successful `get_video_transcript` dual return as backward-compatible formatted JSON text plus the same `structuredContent`;
  - exact transcript `outputSchema`;
  - root `source_url` on every successful transcript result;
  - match-level `timestamp_url` for keyword search;
  - no change to text compatibility, error-only responses, the other seven tools, or Bilibili request count.
- Keep the `[Unreleased]` heading present and empty above v1.8.0.
- README edits are limited to:
  - top release link/text from `v1.7.2` to `v1.8.0`;
  - current development-process test count from `286` to the actual verified full-suite count.
- Do not duplicate the existing structured-output or evidence-link field documentation elsewhere in either README.
- Do not modify any source file, test file, dependency, npm script, package entry point, `.github/workflows/publish.yml`, `dist/`, or public MCP behavior.
- Do not add a changelog claim that publication has happened. This is source preparation only.
- Do not create a branch, commit, push, PR, tag, npm publish, GitHub Release, or workflow dispatch.
- Do not print, persist, fixture, or report Bilibili Cookie values, `SESSDATA`, `bili_jct`, `DedeUserID`, `.env` contents, npm tokens, GitHub tokens, or private credentials.

Preserve and do not edit these pre-existing or Codex-owned working-tree files:

- `docs/agent-memory/active-work.md`
- `docs/agent-memory/pending-learning-proposals.md`
- `docs/qa/2026-07-25-structured-transcript-output.md`
- `docs/research/2026-07-26-v1.8.0-release-github-source-learning.md`
- `docs/agent-memory/handoffs/2026-07-26-v1.8.0-release-prep-codex-to-claude.md`

Codex owns the final release QA, active-work/project-facts/verification/handoff/Harness-eval updates, issue labels/comments, live credentialed Bilibili checks, Git authorization boundary, and any later publication.

## Claude Code Execution Steps

1. Read the ticket, source-learning report, repository rules, and listed files.
2. Record `git status --short`, package/lock versions, the six expected editable files, and the pre-existing dirty files. Do not revert or absorb Codex/user changes.
3. Confirm `package.json` contains no version lifecycle script that would broaden `npm version`.
4. Run:

   ```powershell
   npm version 1.8.0 --no-git-tag-version
   ```

   Confirm it changed only `package.json` and `package-lock.json`, and that dependency entries did not change.
5. Add matching `## [1.8.0] - 2026-07-26` entries to both changelogs beneath the empty `[Unreleased]` section. Keep the claims bounded to Issue #16 and Issue #17.
6. Update both README release links to v1.8.0. Update `286` to the actual full-suite count only after the full suite passes.
7. Invoke `package-maintainer` for package/lock/dependency/package-content review.
8. Run the verification commands below and fix only same-scope release-preparation problems.
9. Invoke `release-verifier` after the files and verification evidence are ready.
10. Inspect `docs/agent-memory/codemap.md`; report `checked unchanged` because ownership and navigation should not change. Do not edit it merely to record the check.
11. Write the Claude report using the repository report template.
12. Stop without performing any Git or publication action.

## Verification Commands

Run and record exact results:

```powershell
node -e "const p=require('./package.json'); const l=require('./package-lock.json'); if(p.version!=='1.8.0'||l.version!=='1.8.0'||l.packages[''].version!=='1.8.0') process.exit(1); console.log('version parity 1.8.0')"
npm run build
npm test
npm audit --omit=dev --json
npm pack --dry-run --json
git diff --check
git status --short
```

Also perform read-only assertions:

- Built MCP server metadata resolves to `1.8.0`.
- The package dry run reports version `1.8.0`, includes `dist/index.js` and package entry points, and contains no `.env`, Cookie, token, `src/`, private handoff, research, or agent-memory files.
- The dry-run package file count does not unexpectedly expand from the current 124-entry baseline.
- `package-lock.json` dependency graph is unchanged except for root package version fields.
- `.github/workflows/publish.yml`, `src/`, and `tests/` have no diff.
- Both READMEs point to the exact v1.8.0 GitHub Release URL and report the same verified test count.
- Both changelogs contain equivalent v1.8.0 public facts and do not claim a completed publication.
- The intended added lines contain no credential assignments, tokens, or private values.

Do not run a real credentialed Bilibili call, tag check, npm publication, workflow dispatch, or published-package smoke. Codex owns those acceptance steps.

## Acceptance Criteria

- [ ] Package, root lockfile, and lockfile root package all report `1.8.0`.
- [ ] The dependency graph, scripts, entry points, source, tests, and publish workflow are unchanged.
- [ ] Chinese and English changelogs accurately cover Issue #16 structured output and Issue #17 evidence links.
- [ ] Both README release links point to v1.8.0 and both show the independently verified current full-suite count.
- [ ] The existing eight tools and transcript output schema remain stable.
- [ ] Build, full tests, production audit, package dry run, package secret scan, and diff check pass.
- [ ] `package-maintainer` and `release-verifier` outcomes are recorded.
- [ ] No source, test, dependency, workflow, Git, tag, npm, or GitHub Release action occurred.
- [ ] The required report truthfully records commands, results, skipped checks, risks, and decisions.

## Risks

- `npm version` can run lifecycle scripts in some packages; inspect scripts first and stop if this repository adds one unexpectedly.
- Release-note drift could overclaim publication or describe unrelated historical work; keep text tied to Issues #16 and #17.
- A package dry run can reveal unintended files even when source diffs look correct; treat any unexpected expansion or secret-like artifact as a blocker.
- npm account-side trusted-publisher configuration can drift independently of the repository; it remains a later release-time check.
- A workflow upgrade may be useful later, but mixing it into this release would invalidate the already-proven release path and broaden the ticket.

## Things To Avoid

- Do not turn this into an Actions modernization, semantic-release migration, dependency cleanup, test refactor, or product-feature ticket.
- Do not update historical v1.7.2 verification facts.
- Do not reformat whole README or changelog sections.
- Do not edit generated learning proposals or promote them.
- Do not infer publication authority from the word “release” in the ticket.

## Stop And Report If

- Any correct preparation appears to require a source, test, dependency, workflow, package-entry, or public-interface change.
- `npm version` changes more than the two package metadata files.
- The full suite does not pass with exactly the current test baseline, or the build/package output differs materially from the current contract.
- The production audit reports a vulnerability that affects release safety.
- The package dry run includes secrets, `.env`, source files, private harness files, or unexpected publishable content.
- Package/lock/changelog/README facts cannot be made consistent without a product decision.
- A credential, workflow permission, npm account, Git, tag, or publication action is required.
- Unrelated pre-existing work prevents a scoped diff.

## Expected Claude Report

Use `docs/agent-memory/agent-communication.md` and include:

- Files changed.
- Exact package/lock dependency-diff result.
- Commands run and exact results, including test count and package file count.
- `package-maintainer` and `release-verifier` outcomes.
- Changelog/README parity findings.
- Security/credential/package-content findings.
- Risks, skipped checks, unresolved boundaries, and decision points.
- Confirmation that no commit, push, tag, workflow dispatch, npm publish, or GitHub Release occurred.
- A `Harness Artifacts` section covering:
  - Task ticket: GitHub Issue #18.
  - Research note: `docs/research/2026-07-26-v1.8.0-release-github-source-learning.md`.
  - QA checklist: Codex-owned after independent verification.
  - Codemap: checked unchanged or updated only with a real ownership reason.
  - Harness security: reviewed for the handoff/report/package boundary.
  - Harness eval: Codex-owned after the release-preparation cycle.
