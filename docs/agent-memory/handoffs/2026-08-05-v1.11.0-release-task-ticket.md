# Task Ticket: v1.11.0 Release

- Date: 2026-08-05
- Status: Complete

## Objective

Publish the verified ASR Phase 1–3, security remediation, documentation, and
README overview Hero as `@xzxzzx/bilibili-mcp` v1.11.0.

## Scope

- Version `package.json` and `package-lock.json` as 1.11.0.
- Commit the complete verified `0a1b` release boundary.
- Exclude the review-gated `docs/agent-memory/pending-learning-proposals.md`.
- Fast-forward `origin/master`, create annotated tag `v1.11.0`, let the pinned
  trusted-publishing workflow publish npm, and create a Chinese GitHub Release.

## Verification

- `npm run build`
- `npm test`
- `npm audit --omit=dev --json`
- `npm pack --dry-run --json --ignore-scripts`
- `git diff --check`
- Value-free secret-pattern classification and ASR temporary-state residue check
- Post-publish GitHub Actions, npm version/provenance, Git tag, GitHub Release,
  and isolated CLI smoke

## Stop Conditions

Stop before tagging or publishing if build, tests, audit, package boundary,
secret classification, release workflow, version uniqueness, or remote
fast-forward checks fail. Never expose Cookie values or other credentials.
