# Codex Fallback Report: v1.8.0 Release Preparation

## Summary

Paseo launched one bounded Claude Code implementation agent with the user-selected `claude/glm-5.2[1m]` provider for GitHub Issue #18. Agent `069a39a6-c109-488a-a29f-c2db5303a851` completed the package-version update, both changelog drafts, and the Chinese README updates, then stopped on provider HTTP 429 before it could finish README_EN, run the required subagents, or write a Claude report.

Codex did not fabricate a Claude report and did not switch to DeepSeek. It inspected the complete Paseo log, corrected the partial-schema wording, completed the same bounded English README changes, and independently ran the remaining package, runtime, security, and release-preparation gates.

After the final independent review, the Paseo agent was archived and GitHub Issue #18 moved from `ready-for-agent` to `ready-for-human`.

## Files Changed

Release-preparation files:

- `package.json`
- `package-lock.json`
- `CHANGELOG.md`
- `CHANGELOG_EN.md`
- `README.md`
- `README_EN.md`

Codex-owned Harness evidence:

- `docs/research/2026-07-26-v1.8.0-release-github-source-learning.md`
- `docs/research/2026-07-26-v1.8.0-production-dependency-advisory-triage.md`
- `docs/qa/2026-07-26-v1.8.0-release-prep.md`
- this fallback report
- `docs/agent-memory/active-work.md`
- `docs/agent-memory/project-facts.md`
- `docs/agent-memory/handoff-log.md`
- `docs/agent-memory/verification-log.md`
- `docs/agent-memory/harness-eval.md`

Pre-existing changes to `docs/agent-memory/pending-learning-proposals.md` and `docs/qa/2026-07-25-structured-transcript-output.md` were preserved. The generated proposal queue was not promoted.

## Commands Run

- Paseo inspection and log review for agent `069a39a6-c109-488a-a29f-c2db5303a851`
- `npm ci`
- `npm run build`
- `npm test`
- `npm audit --omit=dev --json` through the installed npm CLI and npm 11.18.0
- Exact omit-dev dependency inventory submission to the official npm bulk advisory endpoint after the CLI response-decoding failure
- `npm pack --dry-run --json`
- Official MCP SDK `Client` + `StdioClientTransport` calls against local `dist/index.js`
- Safe `node dist/cli.js check`
- version, dependency-graph, package-content, UTF-8, credential-pattern, source/test/workflow-diff, and `git diff --check` assertions
- independent `package-maintainer` and `release-verifier` read-only reviews
- Codex Security `triage-finding/v0` rendering for all four production advisories

## Results

- Package, lockfile root, and lockfile root package all report `1.8.0`.
- The dependency graph, scripts, entry points, `.github/workflows/publish.yml`, `src/`, `tests/`, and public behavior are unchanged.
- Build passed and the compiled MCP server reported version `1.8.0`.
- Full Vitest passed: 23 files / 299 tests.
- Package dry run passed: version `1.8.0`, 124 files, all main/types/bin entries present, and no source, test, `.env`, credential, private handoff, research, QA, or agent-memory file in the package.
- Official SDK discovery returned the existing eight tools in repository-defined order.
- Credential status reported only `configured: true`, `source: global_config`, and `logged_in: true`.
- Ordinary transcript `BV1vL411G7N7` returned the expected source URL and exact legacy-text/structured equality.
- Multi-Part transcript `BV1Eb411u7Fw`, Part 4, query `函数`, returned one bounded match at 1.12 seconds with `?p=4` and `?p=4&t=1.12`; legacy text again equaled `structuredContent`.
- The first SDK assertion used an incorrect locally typed expected tool order. Comparing it with the repository contract identified the harness assertion error; the corrected probe passed without a product change.
- No credential value was printed or stored.

Production dependency gate:

- The normal npm audit command did not return parseable JSON because the official compressed response was not decoded by the tested CLI paths.
- The exact official bulk-advisory response contained four advisories: one moderate Hono finding, one low body-parser finding, and two high fast-uri findings.
- Static source-to-sink triage concluded all four are currently not actionable on this stdio-only, fixed-schema product surface. This is not a claim of a zero-advisory dependency tree.
- Dependency changes remain outside Issue #18 and require a separate bounded ticket.

## Diff Notes

- The six release files contain only version identity, bilingual release notes, release links, and the verified test count.
- Changelog wording says the `outputSchema` matches the complete release result; it does not present the pre-Issue-17 four-field list as the final exact schema.
- The `[Unreleased]` sections remain present and empty.
- No changelog or README claims that v1.8.0 has already been published.
- Codemap was inspected and is unchanged because module ownership, MCP flow, test layout, package layout, and Harness navigation did not change.

## Risks Or Skipped Checks

- No Claude-authored report exists because the GLM provider rejected further requests at its five-hour quota boundary.
- The required Claude Code `package-maintainer` and `release-verifier` subagents did not run before the quota stop. Codex replaced them with two bounded independent read-only reviewers using the same checklists.
- The production dependency graph still contains four advisory matches; their current paths are statically non-exploitable, but they should be refreshed separately and rechecked before publication.
- npm account-side trusted-publisher configuration was not observable from the repository and remains a release-time human check.
- Claude Desktop and Cursor were not tested. They are outside the Issue #18 gate because the official SDK exercised the unchanged local MCP contract.
- No commit, push, tag, workflow dispatch, npm publication, or GitHub Release was performed.
- Paseo agent `069a39a6-c109-488a-a29f-c2db5303a851` was archived after its complete log and failure evidence were recorded.

## Harness Artifacts

- Task ticket: GitHub Issue #18 used.
- Research note: commit-pinned release source-learning and production advisory triage created.
- QA checklist: `docs/qa/2026-07-26-v1.8.0-release-prep.md` created.
- Codemap: checked unchanged.
- Harness security: reviewed for external-source trust, secret exclusion, generated-learning isolation, report truthfulness, and Git/publication boundaries.
- Harness eval: updated by Codex for the v1.8.0 source-preparation cycle.

## Decision Points

- Issue #18 can move to `ready-for-human` as verified source preparation.
- Publication still needs separate explicit authorization and a fresh dependency/trusted-publisher preflight.
- Production dependency refresh is tracked as GitHub Issue #19; it must not be folded into this release-preparation diff.

## Suggested Codex Review Focus

- Confirm that the four advisory verdicts remain valid for the exact transport and schema surface.
- Keep generated learning state out of any later commit.
- Before publication, recheck remote branch/tag/npm state, trusted-publisher configuration, the official production audit, and the v1.8.0 package contents.

## Subsequent Git Delivery

- After explicit user authorization, source-preparation commit `8cad77c` and the separate dependency-remediation commit `2c87750` were pushed to `origin/master`.
- GitHub Issues #18 and #19 were closed as completed.
- No tag, workflow dispatch, npm publication, or GitHub Release was performed.
