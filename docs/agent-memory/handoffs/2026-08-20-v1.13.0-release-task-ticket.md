# Task Ticket: v1.13.0 Creator Discovery Release

## Ticket

- ID: `REL-2026-08-20-V1.13.0`
- Status: `done`
- Owner: Codex
- Source: closed specification #44, merged PRs #49-#52, and the user's release authorization

## Objective

Publish the merged Creator Search and Creator Content Discovery work as the
backward-compatible feature release `v1.13.0`, with synchronized bilingual
README, changelog, and GitHub Release content.

## Scope

In scope:

- Synchronize the five package and server metadata version fields.
- Move the merged Creator work from both Unreleased changelogs into `1.13.0`.
- Refresh the matching Chinese and English README entry points and one bounded usage example.
- Verify the candidate, push the release commit to `master`, publish npm through the existing trusted workflow, push an annotated tag, and create a bilingual GitHub Release.
- Record verified publication facts in project memory after public checks pass.

Out of scope:

- Runtime, dependency, MCP schema, credential, workflow, Official MCP Registry, or unrelated documentation changes.

## Acceptance Criteria

- [x] Five version fields report `1.13.0`; package and Registry names remain consistent.
- [x] Chinese and English README/changelog/release content describe the shipped Creator surface consistently.
- [x] Build, full tests, package dry run, production audit, secret scan, and independent release review pass.
- [x] Annotated `v1.13.0` peels to the release commit on `master`.
- [x] Trusted npm publication succeeds and npm `latest` reports `1.13.0` with provenance.
- [x] GitHub Release `v1.13.0` is public, latest, non-draft, and non-prerelease.
- [x] No credential values or dirty-primary changes enter the release.

## Stop And Report Conditions

Stop before publication if verification fails, `1.13.0` becomes occupied,
remote `master` moves unexpectedly, or the trusted publish workflow fails
without a bounded same-scope repair.
