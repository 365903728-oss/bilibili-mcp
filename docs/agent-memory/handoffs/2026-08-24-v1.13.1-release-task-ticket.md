# Task Ticket: v1.13.1 ASR GPU And Fake-IP Release

## Ticket

- ID: `REL-2026-08-24-V1.13.1`
- Status: `done`
- Owner: Codex
- Source: merged PRs #60-#63 and #68-#71, closed Issues #54-#55 and #57-#59 / #64-#67, plus the user's explicit release authorization

## Objective

Publish the merged Node 25, Fake-IP DNS, ASR Model Tier, Execution Profile,
CUDA readiness, and first-request migration work as `v1.13.1`, with matching
Chinese and English changelogs and GitHub Release content.

## Scope

In scope:

- Synchronize the five package and Registry version fields to `1.13.1`.
- Add matching Chinese and English `1.13.1` changelog sections and bilingual GitHub Release notes.
- Verify and fast-forward the release commit to `master`, then create and push one annotated `v1.13.1` tag.
- Let the existing tag-only GitHub Actions workflow publish npm through trusted OIDC publishing.
- Verify the public package, provenance, GitHub Release, and Official MCP Registry exact/latest records.
- Record only verified publication facts in project memory.

Out of scope:

- Runtime, dependency, workflow, MCP schema, credential, driver, CUDA installation, or unrelated documentation changes.
- Moving an existing tag, force-pushing, storing tokens, or claiming real Linux GPU validation.

## Acceptance Criteria

- [x] Five version fields report `1.13.1`; package and Registry names match exactly.
- [x] Chinese and English changelog and release content describe the shipped behavior consistently.
- [x] Build, full tests, package dry run, production audit, secret scan, Harness checks, and independent release review pass.
- [x] Annotated `v1.13.1` peels to the exact release commit on `master`.
- [x] Trusted npm publication succeeds and npm `latest` reports `1.13.1` with provenance.
- [x] GitHub Release `v1.13.1` is public, latest, non-draft, non-prerelease, and bilingual.
- [x] Official MCP Registry `1.13.1` is active/latest with matching npm metadata.
- [x] The dirty primary checkout remains untouched and no credential value enters Git, npm, GitHub, or the Registry.

## Stop And Report Conditions

Stop before publication if verification fails, `1.13.1` becomes occupied,
remote `master` moves from the frozen release base, the tag exists, or the
trusted publish/Registry state cannot be verified without expanding scope.
