# Task Ticket: v1.11.4 Contract And Search Hardening Release

## Ticket

- ID: `REL-2026-08-09-V1.11.4`
- Title: Publish the merged contract-correctness and search-response fixes as v1.11.4
- Status: `in_progress` — local release preparation passed; authorized remote delivery and publication are in progress
- Owner: `Codex`
- Source: User request to complete the merged work; PR #26 and PR #27

## Objective

Prepare the smallest patch release that publishes the already-merged contract
correctness and malformed-search-response fixes as `1.11.4`, preserves the
ten-tool stdio product boundary, and proves the local candidate before any
irreversible Git or registry action.

## Scope

In scope:

- Bump `package.json`, both root lockfile version fields, and both
  `server.json` version fields from `1.11.3` to `1.11.4`.
- Add matching Chinese and English `1.11.4` changelog sections for PR #26 and
  PR #27.
- Create the release research, QA, and durable verification records.
- Verify the candidate with the exact publish-runner Node/npm versions,
  package/audit/secret gates, public stdio protocol checks, and a credential-
  safe live Bilibili success-path smoke.
- With the separate release authorization received: commit and push the release preparation,
  create and push annotated tag `v1.11.4`, monitor trusted npm publication,
  create the GitHub Release, and publish the Official MCP Registry metadata.

Out of scope:

- Runtime, test, dependency, workflow, README, MCP schema/tool, ASR, or
  unrelated source changes.
- Rewriting merged history, force-pushing, republishing an immutable version,
  or exposing Cookie values.
- Using the stale `publish.bat` or dormant `npm run release` path.

## Files To Inspect Or Edit

Expected release-preparation edit:

- `package.json`
- `package-lock.json`
- `server.json`
- `CHANGELOG.md`
- `CHANGELOG_EN.md`
- `docs/research/2026-08-09-v1.11.4-publishing.md`
- `docs/qa/2026-08-09-v1.11.4-release.md`
- scoped status and verification records under `docs/agent-memory/`

Do not touch:

- `src/`, `tests/`, `.github/workflows/publish.yml`, `README.md`,
  `README_EN.md`, or the dirty primary worktree.

## Acceptance Criteria

- [x] Package, both lockfile fields, and both Registry manifest fields report
      exactly `1.11.4`; `mcpName` still equals `server.json.name`.
- [x] Both changelogs describe only PR #26 and PR #27 and distinguish live
      success-path evidence from automation-backed error/shape branches.
- [x] Exact Node `22.14.0` / npm `11.18.0` clean install, build, all tests,
      stdio, production audit, package dry run, schema, diff/encoding, and
      candidate CLI checks pass.
- [x] Credential-safe live MCP calls prove logged-in search and multi-page
      comments without printing Cookie values.
- [x] Changed-file, tracked-tree, and package-content secret classification is
      complete with no unclassified real credential finding.
- [x] Independent release, standards, and specification reviews accept the complete local
      candidate with no P0-P3 blocker.
- [x] User explicitly authorizes commit, push, tag, npm publication, GitHub
      Release, and Official MCP Registry publication.
- [ ] Annotated `v1.11.4` targets the authorized release commit and the
      tag-triggered Actions run publishes npm `1.11.4` with provenance.
- [ ] npm `latest`, integrity, shasum, provenance, isolated exact-version MCP
      smoke, GitHub Release, and Official Registry `active/latest` state are
      verified after publication.

## Verification

```powershell
npm ci
npm run build
npm test
npm audit --omit=dev --json
npm pack --dry-run --json --ignore-scripts
git diff --check
```

Also validate `server.json` against its live official schema, run the focused
stdio boundary, classify full-development versus production audit findings,
and verify live MCP output without recording credentials.

## Stop And Report Conditions

- Any runtime, test, dependency, workflow, or public-tool change becomes
  necessary.
- Version `1.11.4` or tag `v1.11.4` appears remotely before authorization.
- A build, test, package, production-audit, schema, secret, or live MCP gate
  fails.
- Trusted publishing, exact-version installation, or Registry publication
  fails after the authorized tag is pushed.
