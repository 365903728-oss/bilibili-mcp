# Codex To Claude Handoff: v1.11.1 release preparation

## Objective

Prepare the verified source commit for `@xzxzzx/bilibili-mcp@1.11.1`. Credit
`@CYL-collab` in both changelogs for Issue #24 and PR #25. Do not perform Git
or publication actions; Codex owns commit, push, tag, Actions monitoring, npm
verification, and GitHub Release creation under the user's authorization.

## Current State

- Worktree: `C:\Users\ZX\.codex\worktrees\v111-release\bilibili-mcp`
- Branch: `codex/release-v1.11.1`, tracking `origin/master`.
- HEAD: merge commit `15bb5f84fe51d68dba4026976b26d678198379fa`.
- Issue #24 is closed and PR #25 is merged.
- Remote package and npm `latest` are `1.11.0`; local/remote `v1.11.1` is free.
- The merged production fix is already in `src/bilibili/video-api.ts`; do not
  edit source or tests.
- `README.md` and `README_EN.md` already use `releases/latest`; no README edit
  is needed.
- The current publish workflow is tag-triggered (`v*.*.*`) and uses Node
  22.14.0, npm 11.18.0, OIDC `id-token: write`, tests, build, and
  `npm publish --provenance --access public`. Official npm documentation still
  requires npm >=11.5.1 and Node >=22.14.0 for trusted publishing.

## Files To Inspect

- `AGENTS.md`, `CLAUDE.md`
- `package.json`, `package-lock.json`
- `CHANGELOG.md`, `CHANGELOG_EN.md`
- `.github/workflows/publish.yml` (read-only)
- `docs/templates/research-note.md`
- `docs/templates/release-qa-checklist.md` if present; otherwise use the
  existing release QA records as the local format.
- `docs/agent-memory/README.md`, `active-work.md`, `project-facts.md`,
  `decisions.md`, `lessons-learned.md`, `harness-eval.md`,
  `harness-security.md`, `agent-communication.md`.
- Task ticket:
  `docs/agent-memory/handoffs/2026-08-05-v1.11.1-release-task-ticket.md`.

## Files To Edit

- `package.json`
- `package-lock.json`
- `CHANGELOG.md`
- `CHANGELOG_EN.md`
- `docs/research/2026-08-05-v1.11.1-publishing.md`
- `docs/qa/2026-08-05-v1.11.1-release.md`
- `docs/agent-memory/active-work.md`
- `docs/agent-memory/handoff-log.md`
- `docs/agent-memory/verification-log.md`
- Task ticket status/checkboxes when verified.
- Required report:
  `docs/agent-memory/handoffs/2026-08-05-v1.11.1-release-claude-report.md`.

Do not edit any other file. `project-facts.md`, `decisions.md`, and
`harness-eval.md` will be updated by Codex only after public publication is
verified.

## Required Capability

- Use the `package-maintainer` Claude subagent for the package/version portion
  if available and prompt; if it stalls, take over and report the stall.
- Use the available `secret-scanning` skill for value-free changed-file and
  package-content checks.
- This task only runs existing tests; do not invoke `test-baseline-builder`.
- Do not use product requirements, system design, domain modeling, codebase
  design, or any Superpowers skill; the release scope is already frozen.

## Recommended Approach

Use `npm version 1.11.1 --no-git-tag-version` for consistent package/lockfile
versioning. Add a compact `v1.11.1` section after `Unreleased` in both
changelogs:

- Chinese fix: valid Bilibili AI subtitle numeric IDs above
  `Number.MAX_SAFE_INTEGER` are no longer rejected as invalid metadata; public
  response shape and remaining validation are unchanged. Reference Issue #24
  and PR #25.
- Chinese thanks: thank `[@CYL-collab](https://github.com/CYL-collab)` for
  reporting the issue and contributing the fix.
- English content must match semantically, with an `Acknowledgements` heading.
- Add a verification bullet only from commands actually run.

The external research note must cite current official GitHub tag-filter docs
and npm trusted-publishing/provenance docs, record that the current workflow
already satisfies the required runtime/OIDC boundary, and recommend no workflow
change.

## Things To Avoid

- No source/test/dependency/workflow/README/badge/MCP Toplist changes.
- No manual npm publication, Git commit, push, tag, Release, PR, or Issue write.
- No secret values, full environment output, credentials, or Cookie probes.
- No generated `dist/` edits in the diff.
- No inflated claim that an unsafe integer is exactly preserved as a number;
  exact future identity would require upstream `id_str`.

## Claude Code Execution Steps

1. Read the required project and template files; confirm the exact diff scope.
2. Invoke `package-maintainer`; bump package/lock versions only.
3. Add the matching bilingual changelog fix and contributor credit.
4. Create the compact official-docs research note and release QA checklist.
5. Run `npm ci`, `npm run build`, `npm test`, `npm audit --omit=dev --json`,
   `npm pack --dry-run --json --ignore-scripts`, `git diff --check`, version
   parity, strict UTF-8/replacement-character checks, package path checks, and
   value-free secret classification.
6. Confirm source, tests, dependencies, workflow, README, tool surface, and
   `pending-learning-proposals.md` are unchanged.
7. Update active-work, verification log, handoff log, and task ticket with
   preparation-only verified facts. Do not claim publication.
8. Write the required Claude report using the project template.

## Acceptance Criteria

- Exact task-ticket acceptance criteria for source preparation are satisfied.
- Diff is limited to the listed release-preparation/documentation records.
- `package.json` and both lockfile version fields equal `1.11.1`.
- Both changelogs are clean UTF-8, factually aligned, and credit `@CYL-collab`.
- Build, all tests, production audit, pack boundary, diff, encoding, and secret
  gates pass with exact counts recorded.
- Package dry run includes required dist entry points and public docs, while
  excluding source, tests, QA/research/agent-memory, local config, credentials,
  models/audio, and obsolete Smithery files.

## Risks

- Tagging before the version bump would attempt to republish `1.11.0`.
- A broad stage could accidentally include unrelated files; report the exact
  intended file list.
- Publication is not complete until Actions, npm provenance, exact-version
  install, tag target, and GitHub Release are verified by Codex.

## Stop And Report If

- Any source/test/dependency/workflow/README edit appears necessary.
- Any verification gate fails unexpectedly after one bounded diagnosis.
- Any credential or secret is found in tracked or package files.
- `v1.11.1` is already present locally, remotely, or on npm.

## Expected Claude Report

Use the Claude report template. Include files changed, exact commands/results,
package file/count/size summary, audit and secret results, version parity,
skipped checks, unresolved risks, subagent/skill usage, and complete `Harness
Artifacts` status. State explicitly that no Git or public release action was
performed.
