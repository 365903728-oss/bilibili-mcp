# Task Ticket: v1.12.0 AI Subtitle Integrity Release

## Ticket

- ID: `REL-2026-08-18-V1.12.0`
- Status: `done`
- Owner: Codex
- Source: merged PR #42, closed Issue #40, and the user's release authorization

## Objective

Publish the merged AI subtitle integrity and non-interactive setup changes as
the backward-compatible feature release `v1.12.0` with bilingual changelogs
and GitHub Release notes.

## Scope

In scope:

- Synchronize the five package and Official MCP Registry version fields.
- Move the existing bilingual Unreleased notes into `1.12.0`.
- Correct one stale Chinese README limitation sentence so it matches the
  already-correct English behavior description.
- Verify the release candidate, publish through the existing trusted npm
  workflow, and create a bilingual GitHub Release.

Out of scope:

- Runtime changes, dependency updates, workflow redesign, README rewrites, and
  the future CI/CD roadmap direction.

## Acceptance Criteria

- [x] Build, full tests, package dry run, production audit, secret checks, and
      an independent release review pass.
- [x] Annotated tag `v1.12.0` peels to the release commit on `master`.
- [x] npm `latest` and the public GitHub Release report `1.12.0`.
- [x] No credential values or unrelated dirty-primary changes enter the release.

## Stop And Report Conditions

Stop before publication if the candidate fails verification, the target
version becomes occupied, remote `master` moves unexpectedly, or the trusted
publishing workflow fails without a bounded same-scope repair.
