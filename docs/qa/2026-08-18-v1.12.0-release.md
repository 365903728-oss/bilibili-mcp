# QA: v1.12.0 AI Subtitle Integrity Release

## QA Session

- Date: 2026-08-18
- Owner: Codex
- Related ticket: `REL-2026-08-18-V1.12.0`
- Source: merged PR #42 / closed Issue #40
- Base revision: `0406cee784a4d84443cda00096316ca8174cca44`
- Candidate branch: `codex/release-v1.12.0`
- Release candidate: `1.12.0`

## Scope

- [x] Runtime and tests are the already-reviewed PR #42 merge; release prep
      changes five version fields, bilingual changelogs, one Chinese README
      consistency fix, and release records.
- [x] Both READMEs document the new public behavior; release review corrected
      one stale Chinese limitation sentence without a broad rewrite.
- [x] The user's dirty primary worktree remains outside this isolated worktree.
- [x] No dependency, workflow, tool-count, or future CI/CD roadmap change is included.

## Local Gates

- [x] Remote `master` was `0406cee`; npm `latest` was `1.11.4`; tag, GitHub
      Release, and npm version `1.12.0` were unoccupied at preflight.
- [x] All five version fields report `1.12.0`; Registry and package names match.
- [x] Node `22.14.0` / npm `11.18.0`: build passed; 41 files / 906 tests passed.
- [x] `npm pack --dry-run --json --ignore-scripts`: 189 intended files,
      1,096,273 packed bytes, and required dist entry points present.
- [x] `npm audit --omit=dev`: 97 production dependencies, zero vulnerabilities.
- [x] `git diff --check`, package-path checks, Smithery absence, and scoped
      value-free secret checks passed.

## Publication Gates

- [x] Release verifier returns PASS with no candidate blocker.
- [ ] Release commit is pushed to `master` without force.
- [ ] Annotated `v1.12.0` peels to the release commit and is pushed.
- [ ] Tag-triggered trusted npm publication succeeds with provenance.
- [ ] Exact public package smoke and bilingual GitHub Release verification pass.

## Known Boundary

Stable same-language but semantically unrelated AI subtitle bodies may still
pass the deterministic integrity guard. This is documented and controllable
with `force_asr` or `exclude_ai_subtitles`; this release does not claim general
semantic topic validation.
