# QA: v1.11.4 Contract And Search Hardening Release

## QA Session

- Date: 2026-08-09
- Owner: Codex
- Related ticket: `docs/agent-memory/handoffs/2026-08-09-v1.11.4-release-task-ticket.md` (`REL-2026-08-09-V1.11.4`)
- Source: merged PR #26 and PR #27
- Base revision: `1067e02d3c906d62c4ec1bc48335b8e1dbec70f4`
- Local candidate branch: `codex/release-v1.11.4-prep`
- Release candidate: `1.11.4`
- Release revision: `2a33520739aa96187ad25b36c1e4247dbf8ff640`
- Annotated tag: `v1.11.4` (peeled target equals release revision)
- Publish run: `31296387097` (`success`)
- Published baseline: `1.11.3`
- QA type: `pre-release | MCP tool regression | credential flow | package/install`

## Scope And Boundaries

- [x] Candidate changes are limited to five public release metadata/version
      fields, matching bilingual changelog sections, and scoped release records.
- [x] PR #26 and PR #27 runtime/test source is already merged in the recorded
      base commit; this preparation does not edit `src/`, `tests/`, dependencies,
      the ten-tool schema, READMEs, or the publish workflow.
- [x] The dirty primary worktree remains outside this isolated release worktree.
- [x] Commit, push, tag, Actions, npm, GitHub Release, and Registry writes remain
      outside the local-preparation authorization boundary.

## Preconditions And Version Parity

- [x] Candidate is based directly on `origin/master` at `1067e02`.
- [x] Live remote preflight found no `v1.11.4` tag, GitHub Release, or npm
      version; npm `latest` and the Official Registry remain `1.11.3`.
- [x] `package.json`, both root lockfile fields, top-level `server.json.version`,
      and `server.json.packages[0].version` all report `1.11.4`.
- [x] `package.json.mcpName` equals `server.json.name` exactly:
      `io.github.XZXZZX-Ai/bilibili-mcp`.
- [x] The Registry package identifier remains `@xzxzzx/bilibili-mcp` with
      stdio transport.
- [x] Built CLI `version` reports `1.11.4`.
- [x] MCP `initialize` reports server `bilibili-mcp-server` version `1.11.4`
      with protocol `2025-06-18` under Node `22.14.0`.
- [x] External credentials are loaded only from the approved `global_config`
      source; no Cookie value is copied into this record or command output.

## Exact Publish-Runner Baseline

- Environment: Windows local preflight using Node `22.14.0` and npm `11.18.0`,
  matching `.github/workflows/publish.yml`.
- Clean install: `npm ci` passed with 466 packages installed.
- Build: `npm run build` passed and compiled candidate `1.11.4`.
- Full tests: 41 files / 862 tests passed.
- Focused stdio: 2 files / 19 tests passed.
- Package: 185 files; 1,088,656 packed bytes; 1,718,670 unpacked bytes.
- Production audit: exit 0; 97 production dependencies; zero findings at every
  severity.
- Full development-tree audit: exit 1; 8 transitive dev-only findings
  (1 moderate, 6 high, 1 critical) in `brace-expansion`, `ip-address`,
  `js-yaml`, `nanoid`, `npm`, `postcss`, `tar`, and `undici`.
- Interpretation: production and full-development audits are intentionally
  recorded separately. The eight findings are not production dependency
  findings; dependency maintenance remains separate from this no-dependency
  patch release.

## Package And Registry Metadata

- [x] Dry-run package includes `package.json`, `LICENSE`, `dist/index.js`,
      `dist/index.d.ts`, `dist/cli.js`, and the intended public bilingual docs.
- [x] Dry-run package excludes `src`, tests, QA, research, agent memory,
      `node_modules`, `.env`, `.git`, `.claude`, and `.codex` paths.
- [x] No `.tgz` artifact remains in the worktree after dry-run inspection.
- [x] `server.json` remains outside the npm tarball by existing design; packed
      ownership is represented by `package.json.mcpName`.
- [x] The repository `server.json` validates against the live official
      `2025-12-11` schema at candidate version `1.11.4`.
- [x] `git diff --check` exits zero. All newly added files and candidate-added
      lines are strict UTF-8, introduce no BOM, and add no replacement
      characters. `docs/agent-memory/verification-log.md` retains six
      historical baseline U+FFFD characters; this candidate adds none and does
      not rewrite unrelated history.

## MCP Stdio And Tool Discovery

- [x] Child-process stdio startup keeps the startup notice on stderr and stdout
      JSON-RPC clean.
- [x] `initialize` negotiates protocol `2025-06-18`.
- [x] `tools/list` preserves the exact ten-tool set:
  - `get_credential_setup_instructions`
  - `check_bilibili_credentials`
  - `check_mcp_update`
  - `get_video_info`
  - `get_video_comments`
  - `get_video_transcript`
  - `get_video_metadata`
  - `get_video_chapters`
  - `search_bilibili_videos`
  - `list_bilibili_favorite_videos`
- [x] Public validation and upstream-error responses retain the existing
      text-only error contract.

## Credential-Safe Live MCP Acceptance

- Runtime: Node `22.14.0`, local candidate `dist/index.js`, real MCP stdio.
- Credential status: configured from `global_config`; logged in `true`.
- Search: three sequential `五道口纳什` calls each returned 5 results and each
  contained author `五道口纳什`.
- Representative discovery: `黑神话悟空` returned 5 candidates; public video
  `BV1AE4m1d7XT` exercised comments.
- Comments: with `include_replies=false`, `limit=21` returned 21 and `limit=50`
  returned 50, covering real multi-page aggregation and caller-visible caps.
- Privacy: no Cookie value or private Favorites data was printed or recorded.
- Boundary: live calls covered normal authenticated success paths only.
  Explicit-empty search results, malformed response shapes, HTTP/network
  failures, abort behavior, rejected comment rows, and malformed containers are
  covered by deterministic tests rather than forced against the live service.

## Security And Privacy Checks

- [x] Changed release files, the tracked tree, and the final package file set
      have been scanned with value-free high-confidence credential patterns.
- [x] Package findings for private keys, GitHub/npm/provider tokens, AWS keys,
      JWTs, and literal Bilibili Cookie assignments are zero.
- [x] The only tracked-tree Cookie-shaped match is `tests/cli.test.ts`,
      classified as synthetic test data used with an isolated temporary home;
      the test file is not in the package. The matched value was not recorded.
- [x] No release command accepts or prints a Cookie, npm token, GitHub token,
      `.env` value, or private credential.

## Publication Chain

Explicit user authorization was received on 2026-08-09; every authorized step
completed:

- [x] Create the scoped release commit and fast-forward `origin/master`.
- [x] Create and push annotated tag `v1.11.4` at that release commit.
- [x] Verify the tag-triggered Actions run, tests, build, OIDC npm publish, and
      provenance.
- [x] Verify npm `latest`, integrity, shasum, provenance, and an isolated
      exact-version CLI/MCP smoke for `@xzxzzx/bilibili-mcp@1.11.4`.
- [x] Create a public, non-draft, non-prerelease bilingual GitHub Release.
- [x] After npm is live, publish `server.json` with the Official MCP publisher
      and verify Registry version `1.11.4` as active/latest.

Recorded public evidence:

- npm `latest=1.11.4`; shasum
  `f693ce356c81725c7093e61ce03465c5d041a868`; registry signature present;
  attestation predicate `https://slsa.dev/provenance/v1`.
- Isolated install verified 97 package signatures and 10 attestations. Node
  `22.14.0` exact-package MCP smoke reported server version `1.11.4`, exactly
  ten tools, and a successful authenticated live search without credential
  output.
- GitHub Release:
  `https://github.com/XZXZZX-Ai/bilibili-mcp/releases/tag/v1.11.4`.
- Official publisher `1.8.1` matched its upstream SHA-256 before use. The public
  Registry API reports `1.11.4` as `active` and `isLatest=true`.

## Result

- Overall result: `PASS; v1.11.4 published and publicly verified`
- Blocking issues: none.
- Non-blocking caveats: full development-tree audit has eight transitive
  dev-only findings while the production audit is clean; third-party desktop
  clients remain outside this release acceptance. The successful Actions run
  warns that pinned checkout/setup-node action bundles target deprecated Node
  20 and are forced by GitHub to Node 24, while project commands still use the
  configured Node `22.14.0`. The stale `publish.bat` and dormant semantic-
  release path must not be used.
- Follow-up tickets: dependency-maintenance triage may address the dev-only
  audit findings separately without expanding this patch release.
- Codemap update status: checked and unchanged; release metadata adds no module,
  tool-flow, or navigation seam.
- Harness-security status: reviewed; scoped docs add no automatic execution,
  credential storage, hook, skill, or trust-boundary change.
- Research note: `docs/research/2026-08-09-v1.11.4-publishing.md`.
- Independent review: release-verifier, standards/security, and specification
  reviewers returned PASS with no remaining P0-P3. The release-verifier's one
  initial P2 corrected the historical U+FFFD wording before final acceptance.
