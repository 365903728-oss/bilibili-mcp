# QA: v1.8.0 Structured Transcript Evidence Release Preparation

## QA Session

- Title: v1.8.0 source preparation
- Date: 2026-07-26
- Base revision: `390a5edfa735aa803f607f04e9f90a1ebddc7ba8`
- Prepared package version: `1.8.0`
- Published npm/GitHub version at verification time: `1.7.2`
- Owner: Codex
- Related tickets: GitHub Issues #18 and #19
- QA type: release and MCP compatibility

## Scope

In scope:

- Package and lockfile version parity.
- Bilingual changelog and README release facts for Issues #16, #17, and #19.
- Compatible lockfile refresh for the body-parser and fast-uri advisories.
- Build, full tests, package contents, local stdio contract, credentialed transcript evidence, dependency advisories, UTF-8, secret patterns, and diff integrity.
- Harness handoff, report, research, memory, and evaluation records.

Out of scope:

- New tools, inputs, Bilibili requests, direct dependencies, overrides, SDK/Hono major changes, workflow edits, product behavior, commit, push, tag, npm publication, and GitHub Release.

## Delegation And Harness

- [x] GitHub Issue #18 is the task ticket.
- [x] Commit-pinned GitHub source-learning completed before implementation.
- [x] Current official npm trusted-publishing and GitHub Actions documentation reviewed.
- [x] Paseo launched one bounded `claude/glm-5.2[1m]` agent.
- [x] The GLM agent's HTTP 429 stop is recorded without a fabricated Claude report or provider switch.
- [x] Codex completed a same-scope fallback report.
- [x] Independent package-maintainer and release-verifier checklists completed.
- [x] GitHub Issue #19 and its bounded dependency handoff were used.
- [x] The second GLM attempt produced no model activity; after the user explicitly selected Codex, Codex completed the same bounded task without changing provider policy.
- [x] The inactive second GLM agent was archived after final review.
- [x] Harness security reviewed.
- [x] Codemap checked unchanged.
- [x] Generated learning proposals preserved without promotion.

## Release Files

- [x] `package.json.version` is `1.8.0`.
- [x] `package-lock.json.version` is `1.8.0`.
- [x] `package-lock.json.packages[""].version` is `1.8.0`.
- [x] Only the approved body-parser/fast-uri lockfile closure changed; no direct dependency declaration, override, SDK/Hono version, script, main/module/types/bin, source, test, or workflow diff exists.
- [x] Chinese and English changelogs are equivalent and describe the release-final transcript schema and evidence links.
- [x] Both changelogs keep an empty `[Unreleased]` section and do not claim publication.
- [x] README and README_EN report 299 tests and continue to point to the current published v1.7.2 Release.
- [x] The v1.8.0 Release links are intentionally deferred until the Release exists, preventing a dead link on the default branch.

## Automated Gates

| Gate | Result | Evidence |
|------|--------|----------|
| Clean install | pass | `npm ci` installed 466 packages. The esbuild allowScripts warning did not affect build or tests. |
| TypeScript build | pass | `npm run build`; compiled MCP metadata reports `1.8.0`. |
| Full Vitest | pass | 23 files / 299 tests. |
| Package dry run | pass | 124 files; version `1.8.0`; main/types/bin present; no source, tests, `.env`, credentials, or internal Harness docs. |
| Production dependency advisory gate | pass with one accepted residual | body-parser and both fast-uri advisories are cleared. The only underlying advisory is the non-reachable Hono `serveStatic` finding; high and critical counts are zero. |
| Node 18 shipped runtime | pass | Node 18.20.8 connected to local `dist/index.js` with the official SDK, observed server 1.8.0, and listed all eight tools. |
| UTF-8 | pass | Intended text files decode strictly and contain no replacement characters. |
| Credential/secret patterns | pass | No actual Cookie, `.env` value, token, private key, or credential assignment found in intended additions or package contents. |
| Diff integrity | pass | `git diff --check`; only informational Windows line-ending warnings observed. |

After the compatible lock refresh, `npm audit --omit=dev --json` decoded normally. It reports Hono and the SDK edge as two moderate nodes for one underlying advisory, with zero low, high, or critical findings. This is not a zero-advisory claim.

## MCP Stdio And Credentialed Acceptance

- [x] Official MCP SDK 1.27.1 `Client` + `StdioClientTransport` connected to local `dist/index.js`.
- [x] Server initialization reported version `1.8.0`.
- [x] `tools/list` returned exactly eight tools in repository-defined order:
  1. `get_credential_setup_instructions`
  2. `check_bilibili_credentials`
  3. `check_mcp_update`
  4. `get_video_info`
  5. `get_video_comments`
  6. `get_video_transcript`
  7. `get_video_metadata`
  8. `get_video_chapters`
- [x] The transcript `outputSchema` still requires the complete release-final result, including root `source_url` and match-level `timestamp_url`.
- [x] Safe credential status was `configured: true`, `source: global_config`, and `logged_in: true`.
- [x] Ordinary `BV1vL411G7N7` transcript used subtitles, returned the expected source URL, and preserved exact text/structured equality.
- [x] Multi-Part `BV1Eb411u7Fw`, Part 4, query `函数`, returned one match at 1.12 seconds with exact Part and timestamp URLs and preserved equality.
- [x] `node dist/cli.js check` completed without displaying a Cookie value.
- [x] The same built stdio server and eight-tool discovery passed under Node 18.20.8.

The first inline SDK probe failed only because its local assertion listed the tools in the wrong expected order. The repository's schema test was treated as authoritative; correcting the harness assertion made the unchanged product pass.

The final completion-audit probe likewise first omitted the canonical trailing `/` from its expected Bilibili URL. Existing URL regressions and the Issue #17 QA record were authoritative; correcting only the probe made the current built product pass all discovery, credential, ordinary-video, multi-Part, and text/structured-equality checks.

## Production Advisory Triage

| Advisory | Component | Verdict | Boundary |
|----------|-----------|---------|----------|
| `GHSA-frvp-7c67-39w9` | `@hono/node-server` 1.19.14 | not actionable, high confidence | No HTTP listener or serve-static path; stdio only. |
| `GHSA-v422-hmwv-36x6` | `body-parser` 2.3.0 | resolved | Patched inside Express's existing compatible range. |
| `GHSA-v2hh-gcrm-f6hx` | `fast-uri` 3.1.4 | resolved | Patched inside Ajv's existing compatible range. |
| `GHSA-4c8g-83qw-93j6` | `fast-uri` 3.1.4 | resolved | Patched inside Ajv's existing compatible range. |

Hono remains deliberately unchanged: patched 2.x requires Node 20, while the package promises Node 18 and does not expose the vulnerable export. Open upstream SDK Issues #2531 and #2548 track the range fix.

## Remote And Publication Boundary

- [x] HEAD and `origin/master` were equal at the start of preparation.
- [x] No local or remote `v1.8.0` tag existed.
- [x] No v1.8.0 GitHub Release existed.
- [x] npm did not contain version 1.8.0.
- [x] The current GitHub Release and npm package remained v1.7.2, so both README release links were kept at v1.7.2.
- [x] `.github/workflows/publish.yml` remained unchanged.
- [x] Source commits `8cad77c` and `2c87750` were pushed to `origin/master` after explicit user authorization.
- [x] No tag, workflow dispatch, npm publication, or GitHub Release occurred.
- [ ] Recheck remote state, dependency advisories, and npm trusted-publisher configuration after explicit publication authorization.

## Result

- Overall result: `pass for source preparation`
- Tracker boundary: Issue #18 is closed after commit `8cad77c` reached `origin/master`.
- Dependency tracker boundary: Issue #19 is closed after commit `2c87750` reached `origin/master`; Issue #20 separately tracks the Node engine-floor mismatch.
- Publication status: not authorized and not performed.
- Publication follow-up: change the bilingual README links to v1.8.0 only after the tag/Release exists.
- Residual risk: one underlying moderate Hono advisory remains installed and statically unreachable; three advisories are resolved.
- Client caveat: Claude Desktop and Cursor were not tested; official SDK stdio acceptance is the Issue #18 client gate.
- Codemap: checked unchanged.
- Research:
  - `docs/research/2026-07-26-v1.8.0-release-github-source-learning.md`
  - `docs/research/2026-07-26-v1.8.0-production-dependency-advisory-triage.md`
