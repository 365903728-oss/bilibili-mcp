# QA: v1.10.1 Favorites-First README Release

## QA Session

- Date: 2026-07-27
- Owner: Codex
- Related ticket: `docs/tasks/2026-07-27-readme-favorites-evidence-story.md`
- Base revision before release preparation: `fa3a121`
- Release candidate: `1.10.1`
- Published baseline: `1.10.0`

## Scope And Boundaries

- [x] Include both READMEs, both existing Hero SVGs, bilingual changelogs, package/lock version, the task ticket, and verified release records.
- [x] Preserve runtime source, MCP schemas, tests, dependencies, and the existing tag-triggered trusted-publishing workflow.
- [x] Exclude the review-gated `docs/agent-memory/pending-learning-proposals.md` worktree change.

## Pre-Publish Gates

- [x] Bilingual README audit, 18 local links, 13-level heading parity, strict UTF-8, SVG structure/safety, and 900px/360px previews pass.
- [x] TypeScript build and full Vitest suite pass: 25 files / 405 tests; source, compiled CLI, and built MCP server report `1.10.1`.
- [x] npm package dry-run contains 138 files, including both READMEs and both Hero SVGs, while excluding internal task, memory, QA, source, test, preview, and environment paths.
- [x] Scoped secret scan and `git diff --check` pass; the independent `release-verifier` reports no blocker. Exact staged-file review remains the final commit gate.

## Publication Chain

- [x] Scoped release commit `3aee13d` is pushed to `origin/master`.
- [x] Annotated tag `v1.10.1` resolves to release commit `3aee13d9111feb7342b3a287baf4c1cb81741a04` locally and remotely.
- [x] Tag-triggered GitHub Actions run `30233179604` passes tests, build, and npm trusted publication.
- [x] npm `1.10.1` is `latest` with integrity, shasum, and SLSA provenance metadata.
- [x] Isolated exact-package CLI version/help smoke passes.
- [x] GitHub Release `v1.10.1 — Favorites-First README` is public, non-draft, and non-prerelease.
- [x] Post-publication project-memory and QA records are committed and pushed separately from the immutable release tag.

## Result

- Overall: `@xzxzzx/bilibili-mcp@1.10.1` is npm `latest` with provenance, the matching GitHub Release is public, and the runtime/MCP surface remains unchanged.
- Residual: GitHub repeated the existing Node 20 action-runtime deprecation annotation for `actions/checkout@v4` and `actions/setup-node@v4`, forcing Node 24. This remains separate workflow maintenance.
- Rollback boundary: the release tag is immutable; any defect must be fixed forward with another patch version.
