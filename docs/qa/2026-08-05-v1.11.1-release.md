# QA: v1.11.1 AI Subtitle ID Fix Release

## QA Session

- Date: 2026-08-05
- Owner: Claude Code (preparation); Codex (publication)
- Related ticket: `docs/agent-memory/handoffs/2026-08-05-v1.11.1-release-task-ticket.md` (`REL-2026-08-05-V1.11.1`)
- Source: GitHub Issue #24; merged PR #25; handoff `2026-08-05-v1.11.1-release-codex-to-claude.md`
- Base revision before release preparation: `15bb5f8`
- Release candidate: `1.11.1`
- Published baseline: `1.11.0`
- QA type: `pre-release`

## Scope And Boundaries

- [x] Bump only `package.json` and `package-lock.json` to `1.11.1`; no source, test, dependency, workflow, README, badge, or MCP Toplist change.
- [x] Add matching Chinese and English `v1.11.1` changelog sections that describe only the merged AI subtitle ID fix and credit `@CYL-collab` (Issue #24, PR #25).
- [x] Create the release research note, QA record, and preparation-only memory/handoff records.
- [x] Keep the main user worktree's review-gated `docs/agent-memory/pending-learning-proposals.md` change outside this clean release worktree.
- [x] Preserve the ten-tool MCP surface, dependencies, and the tag-triggered trusted-publishing workflow unchanged.

## Pre-Publish Gates

- [x] Version parity: `package.json` and `package-lock.json` both report `1.11.1`; no dependency or lockfile drift.
- [x] Clean install: `npm ci` succeeds.
- [x] TypeScript build and full Vitest suite pass with exact counts recorded.
- [x] Production dependency audit (`npm audit --omit=dev --json`) passes with exact finding counts recorded.
- [x] `npm pack --dry-run --json --ignore-scripts` includes required `dist` entry points and public docs, and excludes source, tests, QA/research/agent-memory, local config, credentials, models/audio, and obsolete Smithery files.
- [x] `git diff --check` passes; strict UTF-8 and replacement-character checks pass for both changelogs and all new files.
- [x] Value-free secret classification of changed files and package contents finds no credential, Cookie, token, or private value.
- [x] Both changelogs are clean UTF-8 and factually aligned; the Chinese fix wording does not claim exact preservation of unsafe integers as numbers (upstream `id_str` would be required for exact future identity).
- [x] Source, tests, dependencies, workflow, READMEs, tool surface, and `pending-learning-proposals.md` are unchanged.

## Publication Chain

Owned and verified by Codex only, after the user-authorized commit/push/tag step:

- [ ] Scoped release commit containing the `1.11.1` preparation diff is pushed to `origin/master`.
- [ ] Annotated tag `v1.11.1` points to the release commit (the commit containing the version bump), so the tag-triggered run publishes `1.11.1`, not `1.11.0`.
- [ ] Tag-triggered GitHub Actions run passes install, tests, build, and npm trusted publication.
- [ ] npm `1.11.1` is `latest` with integrity, shasum, and SLSA provenance.
- [ ] Isolated exact-version `npx -y @xzxzzx/bilibili-mcp@1.11.1` version/help smoke passes.
- [ ] GitHub Release `v1.11.1` is public, non-draft, non-prerelease, bilingual, and thanks `@CYL-collab`.
- [ ] Post-publication project-memory and QA records are committed and pushed separately from the immutable release tag.

## Result

- Overall: `pass` for source preparation; publication remains pending and is Codex-owned.
- Blocking issues: none in preparation.
- Non-blocking caveats: the GitHub Actions documentation page fetch failed once during research; the current `writing-workflows` page slug was confirmed via a fallback fetch. npm trusted-publishing minimums (npm 11.5.1+ / Node 22.14.0+) were confirmed directly.
- Follow-up tickets: none; publication chain items above remain for Codex.
- Codemap update status: not applicable — no code navigation, MCP tool flow, test layout, or package/release structure changed (version-only bump plus docs).
- Research note link: `docs/research/2026-08-05-v1.11.1-publishing.md` (external GitHub Actions/npm docs facts).
